import { MyHostListenerDirective } from './my-host-listener.directive';

describe('MyHostListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new MyHostListenerDirective();
    expect(directive).toBeTruthy();
  });
});
